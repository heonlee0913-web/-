# 키워드 참고 자료

격주 키워드 리서치 잡이 참고하는 파일이다. 사용 중인 키워드 리서치 툴(블랙키위, 웨어이즈포스트, 네이버 검색광고 키워드도구 등)에서
표를 텍스트로 복사하거나 CSV로 내보낸 뒤 아래에 붙여넣으면, 리서치 잡이 이 데이터를 우선 참고해서 다음 14일치 키워드를 뽑는다.

파일이 비어 있으면(아래 표에 데이터가 없으면) 리서치 잡은 웹 검색만으로 키워드를 추론한다.

## 사용법

1. 키워드 툴에서 표를 드래그해서 복사하거나 CSV로 내보낸다.
2. 아래 표에 붙여넣는다. 열 이름은 최소 "키워드"만 있으면 되고, 검색량/블로그발행량 등 추가 열이 있으면 더 좋다.
3. 저장하면 다음 리서치 잡 실행 시 자동으로 반영된다.

## 키워드 데이터

| 키워드 | PC 검색량 | 모바일 검색량 | 블로그 발행량 | 비고 |
|---|---|---|---|---|
| (여기에 붙여넣기) | | | | |

## 비고

- 이 파일은 붙여넣을 때마다 덮어써도 되고, 누적해도 된다. 리서치 잡은 항상 최신 내용을 읽는다.
- 실제 캡처 이미지 자체는 텍스트 추출 정확도가 낮아 사용하지 않는다. 반드시 텍스트/CSV로 변환해서 넣어달라.
