# 카운트웹 블로그 자동화 시스템 설계

## 0. 실행 현황 (2026-07-01 구축 완료)

- 키워드 큐 DB: https://app.notion.com/p/cb62f3e85494459390339cbe535a1730
- 블로그 초안 DB: https://app.notion.com/p/5018baab478942a0a90bec0badec1636
- 매일 콘텐츠 생성 잡: `countweb-daily-blog-draft` (매일 08:02, 첫 실행 2026-07-02)
- 격주 키워드 리서치 잡: `countweb-biweekly-keyword-research` (매월 1일/15일 07:00, 정확히 14일 간격은 아니고 근사치)
- 키워드 큐에 첫 배치(2026-07-01-batch1) 14개 수동 시딩 완료 (2026-07-02~07-15 배정)
- 키워드 참고 자료 파일: `KEYWORD_REFERENCE.md` — 실제 키워드 툴 데이터를 텍스트/CSV로 붙여넣으면 다음 리서치 잡부터 반영됨

## 1. 목표

- 매일 1개, 네이버 검색 유입 + 문의 전환을 노리는 블로그 **초안**을 자동 생성한다.
- 초안은 절대 자동 발행하지 않는다. 반드시 사람이 검수한 뒤 직접 발행한다.
- 키워드는 2주(14일)에 한 번씩 자동 리서치하여 14일치를 미리 확보하고, 매일 하나씩 소진한다.

## 2. 전체 흐름

```
[격주 잡] 키워드 리서치 (14일마다 1회)
   └─ 노션 "키워드 큐" DB에 14개 항목 적재 (상태: 대기)

[매일 잡] 콘텐츠 생성 (매일 1회)
   ├─ 키워드 큐에서 "대기" 중 가장 빠른 항목 1건 선택
   ├─ WRITING_PROMPT.md 규칙대로 초안 작성
   ├─ 노션 "블로그 초안" DB에 새 페이지 생성 (상태: 검수대기)
   ├─ 키워드 큐 항목 상태를 "사용됨"으로 변경
   └─ 사용자에게 푸시 알림 발송 ("오늘의 초안이 준비됐습니다")

[수동] 사용자 검수 및 발행
   ├─ 노션에서 초안 확인, 문장 수정, 실제 이미지 삽입
   ├─ 검수 통과 시 네이버 블로그 / 아임웹에 직접 게시
   └─ 노션 상태를 "발행완료"로 변경, 발행 링크 기록
```

## 3. 노션 데이터베이스 설계

### 3.1 키워드 큐 (Keyword Queue)

| 속성 | 타입 | 설명 |
|---|---|---|
| 키워드 | 텍스트 | 메인 키워드 |
| 주제 | 텍스트 | 세부 주제/상황 (검색자 고민 각도) |
| 상태 | 셀렉트 | 대기 / 사용됨 |
| 예정일 | 날짜 | 리서치 시점에 하루씩 배정 |
| 배치 | 텍스트 | 몇 번째 격주 배치인지 (예: 2026-07-batch1) |

### 3.2 블로그 초안 (Blog Drafts)

| 속성 | 타입 | 설명 |
|---|---|---|
| 제목 | 텍스트 | 생성된 글 제목 |
| 키워드 | 텍스트 (relation) | 키워드 큐 항목과 연결 |
| 본문 | 페이지 본문 | 실제 글 (이미지 설명 포함) |
| 상태 | 셀렉트 | 검수대기 / 승인 / 발행완료 |
| 생성일 | 날짜 | 자동 생성 시각 |
| 발행링크 | URL | 실제 발행 후 사용자가 채워 넣음 |

## 4. 잡(Job) 상세

### 4.1 키워드 리서치 잡 — 격주(14일) 실행

- **주기**: 14일마다 1회
- **입력**: 카운트웹 업종/타겟 정보, 과거 사용 키워드 이력(중복 방지)
- **처리**
  1. 홈페이지 제작, 웹디자인, 아임웹, 중소기업/소상공인 관련 검색 트렌드·연관 키워드 리서치
  2. 카운트웹 핵심 축과 결합해 14개 키워드-주제 쌍 생성
  3. 키워드 큐 DB의 기존/과거 항목과 중복 여부 체크
  4. 14개를 향후 14일에 하루씩 배정, "대기" 상태로 키워드 큐 DB에 적재
- **출력**: 노션 키워드 큐 DB에 14개 행 추가

### 4.2 콘텐츠 생성 잡 — 매일 실행

- **주기**: 매일 1회 (예: 매일 오전 8시)
- **처리**
  1. 키워드 큐에서 "대기" 상태 중 예정일이 가장 빠른 1건 조회
  2. `WRITING_PROMPT.md` 규칙에 따라 초안 작성 (제목 → 첫 발화 2줄 → 도입부 → 소개 표 → 본문(이미지 설명 포함) → 소제목 2~4개 → 결론 → CTA → 관련 글/연락처/링크)
  3. 노션 블로그 초안 DB에 새 페이지 생성, 상태 "검수대기"
  4. 키워드 큐 해당 항목 상태를 "사용됨"으로 갱신
  5. 사용자에게 푸시 알림 전송
- **예외 처리**: 키워드 큐에 "대기" 항목이 없으면(14일 소진 시) 리서치 잡을 조기 실행하라는 알림을 보낸다

### 4.3 검수 및 발행 — 수동

- 사용자가 노션에서 초안을 확인
- 필요 시 문장 수정, 실제 이미지 파일 삽입 (이미지 설명 placeholder 위치에)
- 검수 통과 시 네이버 블로그 / 아임웹에 직접 게시
- 노션 상태를 "발행완료"로 변경하고 발행 링크 기록

## 5. 실행 환경

- 두 잡 모두 클라우드 예약 작업(스케줄)으로 실행 (로컬 PC 전원 상태와 무관하게 동작)
- 콘텐츠 생성 잡: 매일 지정 시각
- 키워드 리서치 잡: 14일 주기
- Claude 예약 작업(scheduled task) 기능으로 등록

## 6. 안전장치 / 운영 규칙

- 자동 발행은 절대 하지 않는다. 항상 사람 검수를 거친다.
- 키워드 중복 방지: 리서치 시 과거 사용 이력을 반드시 체크한다.
- 생성된 글은 메인 키워드 반복 횟수(6~8회), 글자 수(1000~1800자), 표 항목 수(최대 4개) 규칙을 스스로 점검한다.
- 이미지는 실제 파일이 아니라 위치와 설명을 담은 placeholder로만 삽입한다. 실제 이미지는 사용자가 검수 단계에서 채워 넣는다.

## 7. 다음 구현 단계

1. 노션에 "키워드 큐", "블로그 초안" 데이터베이스 생성
2. `WRITING_PROMPT.md`를 콘텐츠 생성 잡의 작성 규칙으로 연결
3. 예약 작업으로 격주 리서치 잡 + 매일 생성 잡 등록
4. 첫 배치(14개 키워드)를 1회 수동 실행해 큐를 미리 채워둠
5. 1~2주 시범 운영 후 알림/검수 흐름 점검, 필요 시 조정
